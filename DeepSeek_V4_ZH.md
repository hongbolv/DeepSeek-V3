# DeepSeek V4 论文中文梳理

基于仓库中的 [`DeepSeek_V4.pdf`](./DeepSeek_V4.pdf)，这里对论文 **DeepSeek-V4: Towards Highly Efficient Million-Token Context Intelligence** 做一份中文梳理，重点聚焦“它想解决什么问题、做了哪些关键设计、效果如何、还有哪些限制”。

## 1. 这篇文章主要讲了什么？

一句话概括：

> DeepSeek-V4 想解决“大模型一到超长上下文就又贵又慢”的核心瓶颈，在尽量保持能力提升的同时，把上下文长度原生推进到 **100 万 token**。

论文给出了两个预览版模型：

- **DeepSeek-V4-Pro**：总参数 **1.6T**，每个 token 激活 **49B**
- **DeepSeek-V4-Flash**：总参数 **284B**，每个 token 激活 **13B**

两者都支持 **1M context**，并延续了 DeepSeek 系列一贯的 **MoE + 推理强化 + 系统协同优化** 路线。

---

## 2. 论文想解决的核心问题

作者认为，当前大模型的发展同时受到两类需求推动：

- 一类是 **test-time scaling**，也就是让模型在推理时“想得更久”，从而提升复杂任务表现；
- 另一类是 **long-horizon task**，比如复杂 Agent 工作流、跨文档分析、长链条工具使用等。

但传统 attention 的计算和缓存开销，会随着上下文长度快速上升，导致：

- 超长上下文推理成本过高；
- KV Cache 占用太大；
- 推理越长、越难支撑复杂任务；
- 很多“需要长时间思考”的场景，最终会被基础架构成本卡住。

所以，DeepSeek-V4 的主题并不只是“再做一个更强模型”，而是：

> **把“超长上下文 + 强推理 + 可接受成本”这三件事尽量同时成立。**

---

## 3. 模型层面的几项核心创新

### 3.1 延续 V3 的主干，但重点升级长上下文效率

DeepSeek-V4 并不是完全推倒重来，而是在 DeepSeek-V3 的基础上延续：

- Transformer 主体结构
- DeepSeekMoE
- MTP（Multi-Token Prediction，多 token 预测）

在此基础上，重点补了三件事：

1. **mHC（Manifold-Constrained Hyper-Connections）**
2. **混合注意力：CSA + HCA**
3. **Muon 优化器**

这说明 V4 的设计思路不是单纯堆参数，而是围绕“长上下文效率”和“训练稳定性”做系统升级。

### 3.2 混合注意力是全文最关键的改动

这是论文最核心的技术点。

作者提出的混合注意力由两部分组成：

- **CSA（Compressed Sparse Attention）**
- **HCA（Heavily Compressed Attention）**

可以把它理解成：不是直接对所有历史 token 做高成本注意力，而是先对 KV 做压缩，再配合稀疏选择或更激进的压缩策略，显著降低长上下文开销。

如果进一步拆开看，CSA 和 HCA 并不是“两个独立替代方案”，而是一个混合体系里的两种压缩粒度：

- **CSA**：压缩得相对温和，压缩后再做稀疏检索，更强调“在保留信息的同时省算力”
- **HCA**：压缩得更激进，不再做稀疏筛选，更强调“把超长上下文成本压到更低”

两者交错使用，本质上是在做一种分层分工：

- CSA 负责保留更多“可检索的细节”
- HCA 负责提供更强的“全局低成本覆盖”
- 再配合局部滑动窗口分支，补足近邻细粒度信息

#### 3.2.1 CSA 的原理

CSA 可以理解成一个“三段式”流程：

1. **先压缩 KV**
2. **再用轻量 indexer 做 Top-k 稀疏选择**
3. **最后只对选中的压缩块做核心注意力**

它的关键不是简单平均池化，而是**带权压缩**。

论文里，CSA 先从输入 hidden states 里生成两组 KV 候选和两组压缩权重，然后把每 `m` 个 token 压成一个 compressed KV entry。这里的压缩权重不是固定的，而是模型学习出来的；同时还会加入可学习的位置偏置，让压缩后的表示不只是“内容摘要”，还保留一定顺序结构。

另外，CSA 的压缩有一个很关键的细节：**它不是完全独立分块，而是带有重叠信息的压缩**。论文中使用了两组压缩分支 `a / b`，其中一部分会利用当前块，另一部分会利用前一个块的上下文，因此压缩结果不是死板的局部切块摘要，而更像是“向前兼顾一段历史”的软聚合表示。这样做的目的，是尽量减少纯分块压缩带来的边界信息损失。

压缩完成后，CSA 不会让 query 去看所有压缩块，而是先进入一个 **Lightning Indexer**：

- 先把压缩后的 KV 再映射成更轻量的 indexer keys
- 再为每个 query 生成低秩的 indexer queries
- 计算每个 query 对历史压缩块的重要性分数
- 只保留 Top-k 的压缩块进入后续核心注意力

所以 CSA 的核心思想可以概括成：

> **先把历史序列压缩成更短的“目录”，再用一个轻量检索器挑出最相关的目录项，最后只对这些目录项做正式注意力计算。**

这和传统 full attention 最大的不同在于：

- 不是“对所有历史 token 全量关注”
- 也不是“固定模式稀疏”
- 而是“先压缩，再按 query 动态检索”

#### 3.2.2 CSA 的实现过程

如果按实现流程去理解，CSA 大致可以分成下面几步：

1. **输入投影**
   - 输入 hidden states `H`
   - 线性投影得到两组候选 KV 表示 `C_a / C_b`
   - 同时得到两组压缩权重 `Z_a / Z_b`

2. **块级压缩**
   - 以 `m` 个 token 为一个压缩单元
   - 加入可学习位置偏置
   - 通过 softmax 权重，把一段 token 聚合成一个 compressed KV entry

3. **构建稀疏检索器**
   - 对压缩后的 KV 再构建 indexer key
   - 对每个 query token 构建 indexer query
   - 计算 query 到各个压缩块的打分

4. **Top-k 选择**
   - 对每个 query，只选择最相关的 `k` 个压缩块
   - 这样注意力复杂度就不再随全部历史线性膨胀

5. **核心注意力计算**
   - 使用共享 KV 的 MQA（Multi-Query Attention）形式
   - 让多个 query heads 共享压缩后的 key/value
   - 进一步减少存储和计算成本

6. **输出投影**
   - 因为 head 总维度很大，论文没有直接一次性投回 hidden size
   - 而是先做 **grouped output projection**
   - 先分组降维，再汇总到最终输出，降低投影开销

#### 3.2.3 HCA 的原理

如果说 CSA 是“压缩 + 检索”，那 HCA 更像是“**极限压缩 + 直接注意力**”。

HCA 的目标不是尽可能保留更多可检索细节，而是把序列长度压得更狠。论文中用更大的压缩率 `m'`，并且满足 `m' >> m`。也就是说，HCA 每次会把比 CSA 大得多的一段 token 压成一个 KV entry。

因此，HCA 的特点是：

- 压缩更重
- KV cache 更小
- 注意力 FLOPs 更低
- 但细粒度信息保真度相对更依赖压缩表示本身

和 CSA 相比，HCA **不再做 Top-k 稀疏选择**。原因也很直接：
既然已经压缩得非常狠，压缩后的块数本身已经明显变少，再额外做一层稀疏检索的收益就没那么大了，反而会增加系统复杂度。

所以 HCA 的核心思想可以概括成：

> **先用更高压缩率把长序列折叠成非常短的历史表示，再直接在这些重压缩表示上做注意力。**

#### 3.2.4 HCA 的实现过程

HCA 的实现流程整体上比 CSA 更直接：

1. **输入投影**
   - 输入 hidden states `H`
   - 线性投影得到原始 KV 候选 `C`
   - 同时得到压缩权重 `Z`

2. **重压缩**
   - 以 `m'` 个 token 为一组
   - 使用 softmax 权重和可学习位置偏置
   - 聚合成一个 heavily compressed KV entry

3. **直接做核心注意力**
   - 不经过额外 indexer，不做 Top-k 选择
   - query 直接和压缩后的 KV 序列做注意力

4. **共享 KV 的 MQA**
   - 和 CSA 一样，使用共享 KV 的 MQA 降低成本

5. **分组输出投影**
   - 和 CSA 一样，用 grouped output projection 控制输出映射成本

#### 3.2.5 CSA 和 HCA 如何协同

论文真正厉害的地方，不是只提出了 CSA 或 HCA 其中之一，而是把它们做成了 **hybrid attention**。

我的理解是，这个组合是在平衡三件事：

1. **细粒度信息保留**
2. **超长上下文下的总成本**
3. **系统实现复杂度**

其中：

- **CSA** 更像“信息密度较高的压缩检索层”
- **HCA** 更像“覆盖范围更大的全局压缩层”

两者交错后，模型不用把所有层都做成同样的注意力模式，从而避免：

- 全都用 CSA：信息更细，但整体成本仍偏高
- 全都用 HCA：成本更低，但可能损失太多细节

换句话说，V4 的做法不是在“细粒度信息保留”和“超长上下文总成本”里二选一，而是通过不同压缩强度的层间搭配，把这两者做成一个连续折中。

#### 3.2.6 为了让 CSA/HCA 真正可用，论文还补了哪些实现细节

论文里还有几个很关键、但容易被忽略的工程补丁，它们决定了 CSA/HCA 不是纸面方案，而是真能跑起来：

##### （1）Query 和压缩 KV 的额外归一化

在核心注意力前，论文对 query 和压缩后的 KV 做额外 RMSNorm，目的是防止 attention logits 爆掉，并提高训练稳定性。

##### （2）Partial RoPE

CSA/HCA 都只对一部分维度使用 RoPE，而不是整向量全量加位置编码。
原因在于压缩后的 KV 同时承担 key 和 value 的角色，如果不额外处理，位置编码会污染输出。论文通过“只在部分维度加 RoPE，并对输出再做一次相反位置修正”的方式，把相对位置信息保留下来。

##### （3）Sliding Window 分支

压缩注意力有一个天然问题：
如果一个 query 只能看“前面的压缩块”，那它对自己所在块内部其他 token 的细节感知会变差。

为了解决这个问题，论文额外加了一个 **sliding window branch**：

- 为最近的 `n_win` 个 token 保留未压缩的 KV
- 在核心注意力时，把这部分局部 KV 和压缩 KV 一起使用

这样就把“远距离看压缩表示”和“近距离看原始细节”结合起来了。

##### （4）Attention Sink

论文还引入了 attention sink，让某个头的总注意力质量不必强制分配完毕。
这相当于给注意力一个“留白通道”，在一些无须强行关注所有候选项的场景下，会更稳定。

### 这套设计带来的直接收益

论文给出的 1M 上下文场景下估算结果是：

- **DeepSeek-V4-Pro** 相比 **DeepSeek-V3.2**
  - 单 token 推理 FLOPs 约为 **27%**
  - KV Cache 约为 **10%**
- **DeepSeek-V4-Flash** 相比 **DeepSeek-V3.2**
  - 单 token 推理 FLOPs 约为 **10%**
  - KV Cache 约为 **7%**

也就是说，V4 的关键价值不是“上下文从 128K 改成 1M”这么简单，而是：

> **它试图让 1M context 变成一种“日常可用能力”，而不是只能展示、不能落地的能力。**

### 3.3 mHC：增强残差连接

论文引入了 **mHC** 来升级传统 residual connection。

从作者给出的定位来看，它的目标主要是：

- 提升模型表达能力；
- 让深层网络的信息流动更有效；
- 在大规模训练中帮助模型更稳定地学习复杂模式。

这部分不是整篇文章最“吸睛”的地方，但它是支撑 V4 能力上限的重要结构性补强。

### 3.4 Muon 优化器：更快收敛、更稳训练

DeepSeek-V4 训练中，大部分参数使用 **Muon**，少部分模块仍配合 **AdamW**。

作者给出的理由是：

- 收敛更快；
- 训练更稳定；
- 更适合这种超大规模模型。

这也说明 V4 的升级不只是模型结构升级，而是把“优化器”也纳入整体设计。

---

## 4. 系统与基础设施：这是论文里很重的一条线

这篇文章不是只讲模型结构，它非常强调 **系统协同设计**。核心意思是：

> 如果底层训练/推理系统跟不上，模型结构再好也无法真正支撑百万上下文。

文中重点提到几类基础设施优化：

- 为 MoE 设计融合 kernel，尽量重叠 **计算 / 通信 / 内存访问**
- 使用 **TileLang** 提升 kernel 开发效率和运行效率
- 提供 **deterministic / batch-invariant** kernel，保证训练和推理可复现
- 在训练框架里加入更细粒度的 checkpoint 与并行策略
- 在推理框架里重新设计 **异构 KV Cache 布局**
- 支持 **on-disk KV cache**，让共享前缀复用更高效

这里能看出一个很强的信号：  
DeepSeek-V4 并不是“算法团队单点突破”，而是把 **模型、训练框架、推理框架、kernel、存储** 当成一个整体来做。

---

## 5. 预训练阶段：数据更大，也更强调长文档

论文提到，V4 在 V3 数据基础上继续扩展，整体预训练语料超过 **32T tokens**，并重点强化了以下几类数据：

- 数学
- 代码
- Web 数据
- 长文档
- 多语言数据
- 中期训练里引入的 agentic data

其中一个很值得注意的点是：  
作者明确强调了 **长文档数据的整理**，比如科学论文、技术报告等。这和 V4 的目标高度一致——模型不只是要“能吃下很长输入”，还要在长输入里真正提取有效信息。

### 两个模型的大致训练规模

- **V4-Flash**：训练 **32T tokens**
- **V4-Pro**：训练 **33T tokens**

训练长度也是逐步拉升的：

- 从 **4K**
- 到 **16K**
- 再到 **64K**
- 最后到 **1M**

这说明百万上下文不是简单靠位置编码“硬开”，而是在训练中逐步构建出来的。

---

## 6. 训练稳定性：论文公开了两个很有代表性的工程技巧

训练万亿级 MoE 模型，稳定性是非常现实的问题。文中明确提到，他们遇到了明显的 loss spike 和异常值问题。

作者给了两个关键方案：

### 6.1 Anticipatory Routing

核心思想是：

- backbone 的特征计算使用当前参数；
- routing 的索引计算使用历史参数；
- 通过“提前计算并缓存路由结果”，降低路由与主干同步更新带来的不稳定。

作者的结论是，这能有效缓解 loss spike，而且额外时间开销被控制在可接受范围内。

### 6.2 SwiGLU Clamping

他们对 SwiGLU 的部分数值范围进行裁剪，抑制异常值：

- 线性分支裁剪到 `[-10, 10]`
- gate 分支上界裁到 `10`

作者认为，这个简单但实用的方法对训练稳定帮助很大，而且没有明显损害性能。

这部分内容很重要，因为它体现了论文一个很务实的风格：

> 不只是展示最后结果，也公开了在超大规模训练里真实遇到的问题，以及可行但仍待理论解释的应对办法。

---

## 7. 后训练阶段：从“混合 RL”转向“专家训练 + On-Policy Distillation”

这是 V4 另一条很重要的主线。

论文说，后训练阶段整体上延续 V3.2 的路线，但做了一个关键替换：

> 把原来的 mixed RL stage，改成了 **On-Policy Distillation（OPD）** 主导的方案。

作者的做法可以概括成两步：

### 第一步：先分别训练领域专家

按任务方向分别训练专家模型，例如：

- 数学
- 代码
- Agent
- 指令跟随

每个专家一般经历：

1. **SFT**
2. **GRPO 强化学习**

这样先把不同能力单独做强。

### 第二步：再把多个专家蒸馏到统一模型

最终不是保留很多单独专家模型，而是通过 **多教师 OPD** 把这些能力合并到统一模型里。

这体现的是一种“先分化、再统一”的训练思路：

- 先让模型在单领域里尽量学深；
- 再把这些能力整合到一个最终版本中。

---

## 8. 推理模式设计：把“思考强度”做成显式配置

论文里一个非常有产品感的设计，是把 reasoning effort 做成了明确模式：

- **Non-think**
- **Think High**
- **Think Max**

对应理解如下：

- **Non-think**：更快、更直接，适合日常低风险任务
- **Think High**：更慢但更稳，适合复杂分析
- **Think Max**：把推理强度拉满，追求能力边界

特别是 **Think Max**，论文中甚至给出了一个额外注入 system prompt 的策略，明确要求模型更彻底地展开推理。

这说明 V4 的一个重要方向是：

> 不把“是否深度思考”当成隐含行为，而是让它成为可控的推理资源分配方式。

---

## 9. 与 Agent 和工具使用相关的几个关键点

论文在 Agent 方向也做了不少设计，主要包括：

### 9.1 新的工具调用格式

V4 使用了基于特殊 token 的 XML 风格工具调用格式（文中称为 `|DSML|` schema），目标是：

- 降低工具调用格式错误；
- 提升参数传递稳定性；
- 让模型与工具的交互更鲁棒。

### 9.2 Interleaved Thinking

对于有工具调用的场景，V4 会保留跨轮次的 reasoning 痕迹，而不是像旧策略那样在新用户消息到来后把思维过程全部丢掉。

这对长链路 Agent 任务很关键，因为它意味着模型可以：

- 跨轮保留问题求解状态；
- 在复杂任务里减少“每轮都从头想”的浪费；
- 更适合长时任务和多步工具协同。

### 9.3 Quick Instruction

作者把一些辅助任务（例如是否需要搜索、生成搜索 query、识别领域等）设计成特殊 token，对已有 KV Cache 直接复用，减少额外小模型带来的重复 prefilling。

这其实是一个很务实的优化点：

- 降低首 token 延迟（TTFT）
- 减少系统复杂度
- 提升多工具/多阶段对话的响应效率

---

## 10. 论文给出的效果结论

如果只看结论，可以概括成下面几句：

### 10.1 Pro-Max 是当前最强的开源档之一

论文认为 **DeepSeek-V4-Pro-Max** 在开放模型中建立了新的高点，尤其体现在：

- 通用知识
- 推理
- 数学
- 代码
- 长上下文
- 一部分 Agent 能力

它和顶级闭源模型相比，仍有差距，但差距明显缩小。

### 10.2 Flash 的定位不是“弱化版”，而是“高性价比版”

**DeepSeek-V4-Flash** 参数规模小很多，但在给足思考预算时，推理任务表现依然很强。

论文给人的感觉是：

- Pro 更像旗舰能力上限；
- Flash 更像成本和能力之间更激进的平衡点。

### 10.3 长上下文是 V4 最鲜明的标签

从论文整体来看，V4 最突出的不是单一 benchmark 分数，而是：

> 它把“百万上下文下仍然能跑得动、还能保持较强能力”作为核心卖点。

这一点对长文档分析、复杂 agent 工作流、跨轮工具协作都很关键。

---

## 11. 这篇文章最值得关注的地方

如果只抓重点，我认为有四点最值得记住：

1. **百万上下文不是噱头，而是围绕成本、KV Cache 和可用性做了系统设计**
2. **混合注意力（CSA + HCA）是最关键的结构创新**
3. **V4 的进步来自“模型 + 系统 + 训练稳定性 + 后训练”协同，而不是单点 trick**
4. **它明显在为更长链路的 Agent 与 test-time scaling 做准备**

---

## 12. 论文也坦率承认了哪些局限？

作者在结尾没有只讲成绩，也明确提到了问题：

- 为了追求极致长上下文效率，整体架构变得比较复杂；
- 一些稳定性技巧虽然有效，但理论上还没有完全解释清楚；
- 未来还需要继续探索更优雅的稀疏结构、更低延迟部署、多轮 agent、多模态和更强的数据构建。

这意味着 V4 还是一个很强的 **preview version**，它已经展示了方向，但并不意味着架构已经完全定型。

---

## 13. 最后给一个简短总结

如果把全文压缩成一段话，我会这样总结：

> DeepSeek-V4 的核心意义，不只是“参数更大、分数更高”，而是它试图解决大模型迈向下一阶段时最现实的瓶颈：当模型需要更长上下文、更久推理、更复杂工具链路时，怎么把成本压下来、把缓存做小、把系统打通、把能力继续提上去。  
> 从这个角度看，V4 更像是一次面向“长上下文智能体时代”的基础设施级升级。
